# 技术演进与 MySQL 迁移计划｜P0 后置事项

> 文档状态：**规划，不作为当前 P0 开发任务**
> 最后更新：2026 年 9 月 8 日

## 1. 当前技术结论

当前可试玩版本是根目录 H5 Canvas 原型，数据保存在浏览器本地存储。其职责是先验证玩法是否可玩、可理解、可反复体验。

**在 P0 客户端验收完成前，不进行 MySQL 真库接入、账号系统、实时战斗校验、Cocos 迁移或抖音小游戏部署。**

## 2. 当前已有但不启用的预研资产

- `C:\Users\40776\Desktop\小游戏\schema.sql`：早期 MySQL 数据表草案。
- `C:\Users\40776\Desktop\小游戏\server\src\`：NestJS 服务端模块骨架。
- `C:\Users\40776\Desktop\小游戏\client\`：Cocos Creator 脚本骨架。
- `C:\Users\40776\Desktop\小游戏\shared\`：旧数值、宠物和协议草案。
- `C:\Users\40776\Desktop\小游戏\tests\`：旧抓捕概率测试草案。

这些文件不能直接视为 P0 成果，也不能反向决定当前玩法；在迁移前必须按最终 P0 基线重新审查和精简。

## 3. 启动迁移的前置条件

满足以下全部条件后，才可开始 MySQL 迁移评审：

1. `04_P0_QA验收与回归清单.md` 的关键路径稳定通过。
2. 飞剑、怪物、Boss、抓捕、掉落、结算数值已冻结一个测试版本。
3. 用户确认 P0 不再进行大范围玩法重做。
4. 明确用户身份体系、跨设备存档需求和服务器部署环境。
5. 形成一份由开发总控确认的数据字段清单。

## 4. MySQL 最小迁移范围

初期只迁移当前本地存档实际需要的数据：

| 数据 | 来源 | 说明 |
|---|---|---|
| 玩家 ID / 昵称 | 新增 | 账号接入后生成。 |
| 仙石 | `gold` | 用于飞剑祭炼。 |
| 镇妖葫芦数量 | `gourds` | 抓捕库存。 |
| 飞剑等级 / 伤害 | `weaponLevel` / `weaponDamage` | 当前局外成长。 |
| 已获法器列表 | `equipment` | 初期可使用简单 JSON 或实例表。 |
| 已收服寻木青雀 | `pet` | P0 仅一只灵宠。 |
| 通关标记 | `cleared` | 章节进度展示。 |

当前不迁移：体力、门票、炼狱晶核、史诗/深渊、副本排行、多人、公会、商城与付费字段。

## 5. 服务端最小职责（未来）

1. 玩家存档读取与保存。
2. 单局结算奖励写入。
3. 抓捕道具库存校验与成功结果记录。
4. 基础数据校验与异常请求保护。

不做 WebSocket 实时战斗同步；P0 的战斗仍是本地单人流程。是否做更严格的反作弊，需在正式平台版本评审后另行确定。

## 6. 抖音小游戏适配原则（未来）

- 优先目标：抖音小游戏。
- 当前状态：**未启动部署、未接入平台 SDK**。
- 适配顺序：H5 试玩稳定 → 选择正式客户端技术方案 → 适配平台生命周期 / 存档 / 屏幕 → 真机性能测试 → 灰度测试。
- 微信小游戏、iOS、Android 均不是当前 P0 排期内容。

## 7. 迁移评审输出

开始迁移前必须先产出并确认：

1. 最终数据表设计；
2. API 清单与请求/响应样例；
3. 本地存档迁移策略；
4. 错误恢复与回滚方案；
5. 抖音小游戏部署检查表；
6. 单独 Git 分支和可回退节点。
