# AGENTS.md｜修仙御兽：万剑封妖 协作与确认规范

> 文档状态：**当前生效**
> 生效日期：2026 年 9 月 13 日
> 适用范围：本仓库内的全部 AI、开发成员与自动化协作。
> 本文件优先于 `docs/06_AGENTS_AI智能体协作规范.md` 中已归档的 P0 限制；如规则冲突，以本文件、已确认需求和最新开发基线为准。

## 1. 项目身份与当前边界

- 项目：**《修仙御兽：万剑封妖》**，主题为天衍剑宗、万剑真君、御兽收复、妖潮守阵、章节推图与 Boss 抓捕。
- 当前阶段：H5 客户端原型已进入 **P1+**，当前功能范围以 `README.md`、最新章节开发基线和 `docs/07_开发进度与交接日志.md` 为准。
- 当前原型仍是纯客户端：不得未经明确确认接入 MySQL、账号、云存档、真实支付、抖音小游戏 SDK 或服务端结算；“付费”相关内容仅可标注为内测模拟。

## 2. 强制原则：先确认，后开发

用户每次提出**会改变项目内容**的新需求后，AI 必须先发送一份“本次需求确认单”，并等待用户明确授权，才可以开始修改代码、配置、设计内容、测试、文档或 Git 记录。

### 2.1 需要先确认的事项

包括但不限于：

- 新增、删除或修改玩法、战斗规则、抓捕规则、数值、奖励、角色、宠物、装备、剧情、关卡、UI、交互；
- 修复非阻断性问题、重构、调优、资源替换、测试扩展；
- 新增或调整项目文档、任务计划、数据配置、部署、分支与发布内容；
- 任何可能扩大 P0 / P1 / 后续版本范围的工作。

### 2.2 不需要开发确认的事项

以下只读或说明性工作可直接进行，但不得修改仓库内容：

- 查看当前进度、解释现有功能、读取文件、评估方案、回答问题；
- 为形成确认单而进行的必要只读检查；
- 用户明确要求“仅分析 / 不修改 / 不开发”的工作。

### 2.3 明确授权方式

只有在 AI 发出对应确认单后，用户回复以下含义明确的指令，才视为本次确认单获授权：

- “确认”“采用”“开始”“执行”“按此开发”；
- 或明确确认确认单中的范围、选项和修改内容。

“下一步”“继续”等笼统表达，若无法唯一对应上一份未执行确认单，不得视为授权；AI 应重新简要确认要执行的内容。

## 3. 标准需求确认单

每次需要开发时，先以以下格式向用户确认；内容须简洁、可核对、可回退：

```md
## 本次需求确认

- **我理解的目标**：
- **计划修改内容**：
  1.
  2.
- **不会修改的内容**：
- **AI 自行补全项与依据**：
- **风险 / 待确认项**：
- **验收方式**：
- **确认后执行**：回复“确认”即可开始本次范围内的开发。
```

### 3.1 AI 可自行补全的范围

在不改变已确认核心玩法、主题、版本范围与商业规则的前提下，AI 可补全实现细节，例如：布局间距、文案微调、命名、状态提示、容错、测试用例、文件整理与技术实现方式。

所有自行补全的关键假设必须写入确认单；不得以“补全”为名擅自添加新系统、改变玩法目标、提高版本范围或引入真实付费 / 后端依赖。

### 3.2 不清楚时必须集中提问

当需求涉及玩法归属、数值口径、奖励规则、付费、后端、上线平台、剧情结局或验收标准，且现有资料无法唯一判断时：

1. 先列出已理解部分；
2. 一次性提出最少且必要的问题；
3. 未获答复前不开始开发，也不以猜测替代用户决定。

## 4. 阻断故障例外

仅限以下会阻断试玩或造成数据 / 仓库明显损坏的问题，可先进行**最小恢复性修复**：白屏、所有点击失效、构建 / 启动失败、无法打开试玩页、误删关键文件、明显泄露敏感信息。

例外处理要求：

1. 只修复恢复可用性所必需的最小范围，不顺带开发新内容；
2. 修复后立即告知用户原因、改动、影响和验证结果；
3. 仍须补写进度日志、完成测试并创建可回退 Git 节点；
4. 后续优化与扩展仍必须先走确认流程。

## 5. 开发执行与验证规则

用户确认后，AI 才可按确认单执行，并遵守以下要求：

1. 只实现已确认范围；发现范围冲突时暂停并重新发起确认。
2. 保持修仙御兽主题、天衍剑宗设定、万剑真君主角与既定战斗规则一致。
3. 先检查相关现有文档、代码与最近交接记录，避免覆盖稳定玩法或回退已确认内容。
4. 对代码 / 交互改动执行相应静态检查、自动检查或试玩验证；不得将未验证结果写为完成。
5. 不擅自接入 MySQL、账号、云存档、真实支付、抖音 SDK、服务端或未确认的平台能力。
6. 未经用户明确授权，不删除有效功能、历史回档点或重写共享分支历史。

## 6. 进度记录、Git 与交接（每个开发节点必做）

每次完成已确认的开发节点后，必须：

1. 更新 `docs/07_开发进度与交接日志.md`，记录：确认目标、实际修改、验证结果、未完成项 / 风险、下一步建议和 Git 回档点；
2. 运行与变更匹配的检查，并在日志中如实记录结果；
3. 执行格式 / 差异检查；
4. 创建说明清楚的 Git 提交并推送当前开发分支；
5. 向用户反馈已完成内容、验证情况、修改文件和可回退提交哈希。

建议提交格式：`feat(scope): ...`、`fix(scope): ...`、`docs: ...`、`test(scope): ...`。开发分支默认使用 `codex/` 前缀。

## 7. 文档优先级

出现冲突时，按以下顺序处理：

1. 用户在当前对话中**最新且已确认**的明确决定；
2. 本文件的“先确认、后开发”流程；
3. `README.md` 的当前边界与最新章节开发基线；
4. `docs/07_开发进度与交接日志.md` 的最新已完成记录；
5. 其他设计、数值、规划和归档文档。

旧 P0 文档可作为历史参考，但不得以其过期限制阻止已确认的 P1 / P1+ 内容；遇到冲突必须在确认单中说明。

## 8. 完成反馈模板

每个已确认节点完成后，使用以下交接格式反馈：

```text
🔄 AI 工作交接卡
- 本次确认目标：
- 实际完成：
- 已验证：
- 修改文件：
- 遗留 / 风险：
- 下一步建议：
- Git 回档点：
```

---

**执行提醒：**从本文件生效后，除阻断故障的最小修复外，任何仓库修改都必须先提交确认单并等待用户确认。
