# AGENTS.md｜《修仙御兽：万剑封妖》AI 开发守则

> 状态：当前生效
> 最后更新：2026 年 9 月 13 日
> 适用范围：`C:\Users\40776\Desktop\小游戏` 项目内所有 AI 开发工作。

## 0. 执行前确认

凡是会修改代码、配置、UI、文档、测试或 Git 内容的新需求，必须先向用户发出简明的“本次需求确认单”，得到“确认 / 开始 / 执行 / 按此开发”等明确授权后才可实施。

确认单至少说明：目标、计划修改内容、不会修改的内容、AI 自行补全项、验证方式和待确认问题。白屏、无法点击、无法启动等阻断试玩的问题，仅可先做最小恢复修复，并在修复后立即说明和记录。

## 1. 修改前必读文件

每次修改前必须阅读：

1. `README.md`；
2. `项目开发计划书.md`。

`项目开发计划书.md` 是当前统一计划入口。每次修改前除阅读该文件外，还须阅读与本次功能直接相关的当前基线、验收报告或最新交接记录；`docs/archive/` 中的资料仅用于历史追溯，不得覆盖已确认范围。

## 2. 范围与变更控制

1. 每一轮只完成**一个明确功能**。功能未完成、未测试或范围不清时，不得并行加入其他功能。
2. 不得擅自改变已确认的游戏规则、抓捕概率、数值、奖励、角色能力、关卡流程和现有页面排版。
3. 涉及玩法、概率、数值、奖励、商业化、后端或上线平台的歧义，必须集中向用户提问；不得用猜测替代确认。
4. 仅可在用户已确认的范围内补全实现细节，例如容错、状态文案、命名、测试用例和小范围样式修正；关键补全项必须在确认单中说明。

## 3. 概率与数值管理

1. 概率和数值优先集中在 `js/data.js`。
2. 当前项目未使用 `js/data.js`，现有静态数值配置位于 `p1-data.js`；在未获得用户确认前，不得为了迁移而新建 `js/data.js`、移动配置或改变数值结构。
3. 修改概率或数值前，必须在确认单中列出旧值、拟改值、影响范围与验收方法；未经确认不得调整。

## 4. 分层检查

UI、游戏逻辑和测试必须分开检查、分开说明：

- **UI 检查：**布局、可读性、交互点击 / 触屏、不同页面状态和现有排版是否保持；
- **游戏逻辑检查：**规则、状态机、存档、奖励、数值引用与边界条件；
- **测试检查：**自动化测试覆盖、执行结果及失败原因。

不得以“页面能打开”替代逻辑验证，也不得以自动化测试通过替代 UI / 交互检查。

## 5. 子 Agent 规则

1. 小修改不得创建子 Agent。
2. 大任务可以使用子 Agent，但必须先拆分为互不重叠的文件写入范围；禁止多个 Agent 同时修改同一个文件。
3. 测试 Agent 默认只读，只负责检查、复现、报告风险和提出建议；不得直接修复代码，除非用户明确授权其修复。

## 6. 测试与完成标准

1. 修改游戏逻辑后，必须新增或更新与该逻辑对应的自动化测试。
2. 完成后必须运行：

   ```powershell
   node .\tests\test.js
   ```

3. 当前仓库尚未建立 `tests/test.js`。在该入口经用户确认创建前，临时依次运行 README 中列出的现有六项自动化检查：

   ```powershell
   node .\tests\p0-regression-check.js
   node .\tests\p1-chapter-progression-check.js
   node .\tests\p1-equipment-check.js
   node .\tests\p1-full-chapter-check.js
   node .\tests\p1-chapter2-check.js
   node .\tests\p1-combat-survival-check.js
   ```

4. 测试失败、检查未完成或存在未解释的异常时，不得宣布开发完成，不得将未验证功能描述为可用。
5. 稳定功能在相关检查全部通过后，必须创建 Git 提交；未经明确授权，不得重写历史或删除稳定回档点。

## 7. 最终汇报格式

每次完成后，最终回复只汇报以下四项：

1. **完成内容**；
2. **修改文件**；
3. **测试结果**；
4. **待确认问题**。

除非用户要求，不展开无关过程、命令输出或额外开发建议。
