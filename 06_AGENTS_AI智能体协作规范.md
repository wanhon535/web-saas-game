# AGENTS.md｜修仙御兽：万剑封妖 P0 协作规范

> 文档状态：**生效**
> 最后更新：2026 年 9 月 9 日

## 1. 总控原则

开发总控负责维护 P0 边界、玩法一致性、版本回退与最终验收。所有成员必须以 `01_P0_产品与开发基线.md` 为唯一需求依据，不得依据已经删除的旧 GDD、旧客户端方案或旧服务端任务包自行扩展内容。

## 2. 当前角色与允许修改范围

| 角色 | 主要职责 | 当前允许修改范围 |
|---|---|---|
| 开发总控 | P0 边界、排期、评审、整合、回档、交接 | 全项目；优先维护根目录 MD 与 H5 原型。 |
| 客户端成员 | H5 战斗、交互、主题 UI、局外展示与本地存档 | `index.html`、`p0.css`、`p0.js`。 |
| 数值 / QA 成员 | 数值建议、试玩验收、回归记录 | `02_P0_数值与产出配置说明.md`、`04_P0_QA验收与回归清单.md`、`tests/`。 |
| 服务端成员 | P0 验收后进行 MySQL 迁移预研 | `server/`、`schema.sql`；未收到启动指令前不得扩展旧系统。 |

## 3. P0 红线

1. 不增加多角色、职业切换、六战位、炮台、多灵宠出战。
2. 不增加史诗、深渊、第二章、商城、公会、抽卡、广告、排行或复杂合成。
3. 不把 Cocos、NestJS、MySQL、WebSocket 或抖音 SDK 当作当前 H5 P0 的前置工作。
4. 不擅自改写抓捕、掉落、结算规则；数值改动必须先更新 `02`。
5. 不得删除可回退节点、强制覆盖历史版本或跳过 Git 提交。

## 4. 标准工作步骤

1. 阅读 `00`、`01`、本职责文档和最近的 `07` 日志。
2. 明确本次工作是否属于 P0；不确定时先向开发总控提问。
3. 先更新设计/数值文档，再实施代码修改。
4. 完成后执行对应 QA；界面或交互改动至少完整试玩一局。
5. 在 `07_开发进度与交接日志.md` 记录完成内容、验证结论、遗留项和回档提交。
6. 每一个可试玩节点单独提交 Git。

## 5. 文档优先级

1. `01_P0_产品与开发基线.md`
2. `02_P0_数值与产出配置说明.md`
3. `03_P0_客户端实现与UI规范.md`
4. `04_P0_QA验收与回归清单.md`
5. `07_开发进度与交接日志.md`
6. 其他规划文档

如出现冲突，按更高优先级文档执行，并立即在 `07` 记录待解决问题。

## 6. 标准交接卡

每次阶段性完成后，回复与日志中应包含：

```text
🔄 AI 工作交接卡
- 当前角色：
- 本次完成：
- 已验证：
- 遗留 / 风险：
- 下一步建议：
- 修改文件：
- Git 回档点：
```

## 7. Git 规则

- 开发分支前缀使用 `codex/`。
- 提交说明必须能说明变更目的，例如：`feat: refine P0 boss capture feedback`。
- 未经确认不得重写主分支历史。
- 涉及玩法或数值的提交，必须同时包含相关文档或日志记录。
