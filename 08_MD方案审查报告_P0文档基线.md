# MD 方案审查报告｜P0 文档基线

> 审查日期：2026 年 9 月 8 日  
> 审查范围：项目根目录全部 9 份 Markdown 文档，并抽样核对当前 H5 原型、配置、服务端骨架与 Git 状态。  
> 当前代码分支：`codex/p0-single-hero-redesign`  
> 当前可回档节点：`8a67a1c`（P0 可玩闭环）、`289cc2a`（此前进度记录）

## 一、审查结论

### 结论摘要
**现有文档“有材料，但不是一套可以直接指导 P0 联合开发的完整方案”。**

- 文档体系完整度评分：**43 / 100**。
- 当前 P0 真实方向在 `00_README_项目总览与AI交接指南.md` 与 `07_开发进度与交接日志.md` 中表达较清楚；这两份合计可作为临时开发依据。
- 但旧 GDD、总策划、技术架构、客户端/服务端/QA 指令包仍保留大量“大而全”内容，并且部分内容已与当前原型和 P0 决策相冲突。
- 在未整理为“唯一事实源”前，不应把现有全部 MD 直接交给不同成员分别执行；否则很容易再次出现玩法被改回多角色、多副本、复杂养成的偏题问题。

### 是否可支撑当前工作
| 使用场景 | 结论 | 原因 |
|---|---|---|
| 单人继续优化当前 H5 P0 原型 | **可以，但仅以 00 + 07 为准** | 当前玩法、范围和已实现功能已有说明。 |
| 多成员并行继续开发 P0 | **暂不建议直接启动** | 不同文档给出的玩法、技术与任务边界互相矛盾。 |
| 开始 MySQL / 后端联调 | **暂不具备验收条件** | 数据表、接口、抓捕概率和当前客户端实际逻辑未统一。 |
| 抖音小游戏部署 | **暂不具备** | 当前是 H5 Canvas 原型；Cocos/抖音构建文档为规划或骨架，尚非真实构建链。 |

## 二、当前 P0 唯一事实（后续所有文档必须服从）

```text
游戏：修仙御兽：万剑封妖
阶段：P0
唯一关卡：第一章「幽冥荒原」普通副本
核心玩法：万剑真君单角色守线；初始仅 1 柄飞剑自动攻击
失败条件：妖物越过护宗大阵底线，扣除护宗大阵 HP；HP 归零则失守
波次：3 波小妖 → Boss「寻木青雀」
掉落：仙石、法器；进入吸附范围后自动获得
抓捕：Boss 虚弱时可尝试抓捕
抓捕成功：获得寻木青雀并结算
抓捕失败：Boss 狂暴、回复部分生命、继续战斗；击杀狂暴 Boss 获得额外掉落
局外内容：飞剑祭炼、法器展示、灵宠展示；浏览器本地存档
明确不做：多角色/多职业、多宠物出战、史诗/深渊、商城、公会、抽卡、复杂合成
数据阶段：先 localStorage 验收 P0；通过后再接入 MySQL
平台：首期目标为抖音小游戏，但当前不部署
```

## 三、逐份文档评估

| 文档 | 评价 | 当前处理建议 |
|---|---|---|
| `00_README_项目总览与AI交接指南.md` | **可保留**。范围、玩法、代码入口和 Git 回档点基本正确。 | 升级为入口索引；补充“唯一事实源”链接、运行方式和文档状态。 |
| `01_修仙御兽_万剑封妖_游戏策划完整方案.md` | **大部分不适用于 P0**。包含三职业、双宠、史诗/深渊、团本、3 合 1、炼狱晶核等。 | 保留为“长期愿景 / P1+”，不要再作为 P0 执行依据。 |
| `修仙御兽_万剑封妖_游戏设计方案GDD.md` | **与 P0 冲突最多**。P0 测试范围仍写多流派、三阶副本、双宠、复杂抓捕交互。 | 归档为“旧版 GDD / 长期构想”，或重写为长期 GDD。 |
| `02_修仙御兽_万剑封妖_技术架构与协作总方案.md` | **方向可参考，现状描述失真**。文档定义 Cocos + NestJS + WebSocket，但当前可玩版本为 H5 Canvas + 本地静态服务。 | 拆成“当前原型架构”和“正式版技术演进”，删除 P0 中不需要的 WebSocket、复杂模块。 |
| `03_客户端开发.md` | **已过时且明显偏题**。写成六战位、第二章、史诗、多个运营入口，与当前单角色 P0 相反。 | 必须重写；不能继续给客户端成员作为启动指令。 |
| `04_服务端开发.md` | **任务包清晰，但范围过大且假设已完成**。含 50 晶核保底、实际 MySQL 已接入等旧结论。 | 缩减为“P0 后置服务端预研”；当前只保留数据模型和联调前置条件。 |
| `05_数值与QA测试.md` | **职责明确但测试范围不完整**。只验证旧抓捕道具概率，不覆盖当前战斗、守线、掉落、结算。 | 重写为当前 P0 数值表与冒烟/回归用例。 |
| `06_AGENTS_AI智能体协作规范.md` | **原则可保留，目录和边界需校正**。写入路径与根目录 H5 实际入口脱节，且“抓捕必须服务端判定”不适合当前离线试玩阶段。 | 保留协作红线；补充 H5 P0 可改范围、文档更新责任和 Git 提交要求。 |
| `07_开发进度与交接日志.md` | **当前最可靠的实施记录**。但缺少本次审查记录与明确的待决项清单。 | 持续维护；本次审查已追加记录。 |

## 四、已确认的关键矛盾

### 1. 玩法范围矛盾（高优先级）
- 当前 P0 是**单角色、单飞剑、单普通副本**；旧策划与旧 GDD却要求三职业、双宠、三阶副本、团本、合成保底。
- `03_客户端开发.md` 仍描述 6 个修仙战位、第二章与史诗副本，这正是此前被用户否定的偏题玩法。

### 2. 技术路线矛盾（高优先级）
- 当前真正可运行的是根目录 `index.html + p0.js + p0.css` 的 H5 Canvas 原型。
- `02` 把 Cocos Creator、NestJS、WebSocket 写成当前既定实施架构；实际 `client` 与 `server/src` 主要仍是骨架，不能代表当前试玩版本。
- 不能让成员把“迁移 Cocos”误解为当前 P0 的默认任务。

### 3. 数值与抓捕规则矛盾（高优先级）
- 当前试玩记录：飞剑伤害 16、间隔 1.65 秒、Boss 在 35% 生命触发抓捕。
- 旧 GDD：Boss 25% 生命、停止移动和攻击后点击 Boss、四种抓捕道具。
- 当前 H5 原型仅有两种抓捕选择及独立概率；服务端骨架/QA 配置又使用四道具和不同概率。
- 这些规则没有“哪份为准”的标准数值表，无法可靠调优或联调。

### 4. 产出与成长矛盾（中高优先级）
- P0 当前实际产出是仙石、法器、灵宠和飞剑祭炼。
- 旧文档引入门票、体力、炼狱晶核、3 合 1、装备品阶、保底合成；会不必要地膨胀 P0。
- `schema.sql` 和服务端模拟数据仍承载了旧产出，和 P0 后置 MySQL 的顺序不一致。

### 5. 平台目标表述不统一（中优先级）
- 当前决定是：**优先目标抖音小游戏，但现在不部署**。
- 旧策划写“微信 / 抖音首发”，旧客户端包还有微信构建任务。后续应统一为“抖音优先、H5 原型验证中、未进入平台适配”。

## 五、缺失的关键内容

以下内容是把文档体系提升为可执行 P0 的必要缺口：

1. **P0 产品与开发基线（唯一事实源）**
   - 一页说清范围、非范围、完整流程、状态机、奖励、失败与存档。
2. **P0 数值与产出表**
   - 玩家攻击间隔/伤害、怪物数量/血量/速度、Boss 阶段、阵线 HP、掉落概率/价值、单局目标时长。
3. **P0 验收标准与测试清单**
   - 至少包含：开局、三波、越线扣血、自动吸附、抓捕成功、抓捕失败狂暴、击杀、失败结算、刷新后存档保留。
4. **当前原型运行与发布说明**
   - 环境要求、启动步骤、试玩入口、浏览器兼容范围、清档办法、常见故障与检查方式。
5. **配置归属与变更规则**
   - 哪个文件是数值源、改数值后如何验证、禁止在文案/代码/服务端各自写一套互相矛盾的数据。
6. **P0 → MySQL 的迁移前置清单**
   - 明确哪些存档字段需要迁移、何时接入、接口何时启用；避免服务端先行开发旧系统。
7. **UI 设计约束说明**
   - 明确可借鉴的参考维度（竖屏层级、厚描边、金色主按钮）以及必须保留的国风修仙御兽主题，防止再次套用西幻模板内容。

## 六、推荐的文档治理结构

建议下一次整理时采用下列结构，并在标题中标注 `状态：生效 / 归档` 与最后更新日期：

```text
00_README_项目总览与AI交接指南.md             ← 文档入口、运行入口、唯一事实源链接
01_P0_产品与开发基线.md                        ← 唯一可执行需求
02_P0_数值与产出配置说明.md                    ← 数值源、掉落和调优目标
03_P0_客户端实现与UI规范.md                    ← 当前 H5 原型结构与界面约束
04_P0_QA验收与回归清单.md                      ← 可重复验收
05_技术演进与MySQL迁移计划.md                  ← P0 验收后才启用
06_AGENTS_AI智能体协作规范.md                  ← 写入范围、协作、Git、交接格式
07_开发进度与交接日志.md                       ← 每次进度必记
archive/
  ├── 旧版_GDD_长期规划.md
  ├── 旧版_完整策划_长期规划.md
  └── 旧版_Cocos与服务端远期架构.md
```

> 原则：`01_P0_产品与开发基线.md` 是唯一事实源；若其他文档冲突，一律以它为准。长期规划只能说明未来可能做什么，不能写成当前 P0 任务。

## 七、建议的整改优先级

### P0-A｜先做，避免再次偏题
1. 新建唯一 P0 基线文档。
2. 将 `03_客户端开发.md` 重写为单角色「幽冥荒原」客户端规范。
3. 将旧 GDD / 完整策划标题与首段明确标注为“长期规划，不作为 P0 执行依据”。
4. 统一抓捕阈值、道具数量、概率、失败补救和奖励规则。

### P0-B｜P0 完整试玩前补齐
1. 建立数值与产出表，设定单局时长和体验目标。
2. 建立可手工执行的验收与回归清单。
3. 补充运行说明和清档说明。

### P0-C｜完成客户端验收后再做
1. 梳理 localStorage 到 MySQL 的迁移字段。
2. 按 P0 最终规则精简服务端接口和数据库。
3. 再评估 Cocos/抖音小游戏适配路径，避免现在重复建设。

## 八、审查后的唯一行动建议

**先不新增游戏内容，也不启动服务端大开发。**

应先完成“文档校正与唯一 P0 基线”这一项治理工作，再按基线继续优化战斗、掉落、装备和抓捕手感。这样可把所有成员的理解收敛到同一套 P0 目标，避免后续再次发生内容偏题或回退成本。
